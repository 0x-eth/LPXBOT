{
  "name": "@lpbot/test-fixtures",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared deterministic fixtures for LPBot tests.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --max-warnings=0",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@lpbot/api-contract": "workspace:*",
    "@lpbot/chain-registry": "workspace:*",
    "@lpbot/domain": "workspace:*"
  }
}
